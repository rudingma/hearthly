# Add files here to ignore them from prettier formatting
/dist
/coverage
/.nx/cache
/.nx/workspace-data
.angular

# Helm chart templates — Go template syntax ({{ .Values... }}) breaks Prettier's YAML parser
infrastructure/**/templates/
apps/*/deploy/chart/templates/

# Generated code
apps/hearthly-app/src/generated/
apps/hearthly-api/src/schema.gql

# Lockfiles
bun.lock

# Superpowers-local artifacts (gitignored, mirrored here so format:check skips them)
docs/superpowers/

# Mirror of .gitignore — Terraform state and local cluster artifacts
.terraform/
*.tfstate
*.tfstate.backup
infrastructure/cluster/k3s_kubeconfig.yaml
infrastructure/cluster/k3s_kustomization_backup.yaml

# Scratch dir for AI working artifacts (gitignored — see CLAUDE.md)
tmp/
