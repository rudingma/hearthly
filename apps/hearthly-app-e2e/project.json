{
  "name": "hearthly-app-e2e",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "sourceRoot": "apps/hearthly-app-e2e/src",
  "implicitDependencies": ["hearthly-app"],
  "// targets": "to see all targets run: nx show project hearthly-app-e2e --web",
  "targets": {
    "typecheck": {
      "executor": "nx:run-commands",
      "cache": true,
      "inputs": ["default", "^production"],
      "options": {
        "cwd": "apps/hearthly-app-e2e",
        "command": "tsc -p tsconfig.json --noEmit"
      }
    }
  }
}
