apiVersion: v2
name: monitoring
version: 0.1.0
description: Infrastructure monitoring — Prometheus + Grafana (kube-prometheus-stack)
dependencies:
  - name: kube-prometheus-stack
    version: "82.16.0"
    repository: "https://prometheus-community.github.io/helm-charts"
